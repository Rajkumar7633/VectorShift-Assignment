# Frontend

React pipeline builder for the VectorShift technical assessment.

**Full setup and run instructions:** see the [project README](../README.md) in the repo root.

```bash
npm install
npm start
```

Runs at http://localhost:3000 (requires the backend on port 8000).
